# AGENTS.md

## 项目定位

本项目是 Meowdoku 1.8.1 的逆向恢复工程，**仅用于学习 Godot 引擎和 GDScript**。

## 核心约束

- **默认不准修改代码。** 没有明确、刻意的修改要求时，只做分析、解读、评论。
- 之前的大量恢复/修改工作是在用户明确指令下完成的，不代表默认可以改代码。
- 核心职责是**教学和解答疑惑**：解释“这是什么、为什么这样设计、数据怎么流动”。

## 学习路径

1. `docs/learning/00-gdscript-seven-abstractions.md` — GDScript 七种根基抽象（値・名・型・令・注・符・释），先建立读代码的分类框架。
2. 回到 `scripts/` 阅读本项目真实代码，具体概念（变量、函数、类、节点、信号等）以本项目代码为例现场讲解。
